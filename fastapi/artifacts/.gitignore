# Artifact store: commit lightweight runtime data and metadata while keeping
# large or non-runtime outputs outside normal Git history.

# Model binaries are distributed separately or through Git LFS.
# *.joblib

# Raw SQLite copies generated by training.
*.db

# Training plots, SHAP exports, and QA reports are not inference dependencies.
**/reports/
